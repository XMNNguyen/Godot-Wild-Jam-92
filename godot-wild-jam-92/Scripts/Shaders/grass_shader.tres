[gd_resource type="ShaderMaterial" format=3 uid="uid://dbfrkpyr1epl7"]

[ext_resource type="Texture2D" uid="uid://bnd1wwehha4b1" path="res://Art+Sprites/Assets/Platform_color.png" id="2_e6ndn"]

[sub_resource type="Shader" id="Shader_tdedc"]
code = "shader_type spatial;
render_mode depth_prepass_alpha;

uniform sampler2D main_texture;
uniform sampler2D noise_texture;
uniform sampler2D cloud_noise_texture;
uniform float noise_strength : hint_range(0.0, 1.0) = 0.8;
uniform vec2 cloud_dir = vec2(1.0, 0.0);
uniform float scroll_speed = 0.02;

uniform float texture_scale = 0.01;
uniform float blend_sharpness = 4.0;

varying vec3 world_position;

void vertex() {
	// Calculate world position
	world_position = (MODEL_MATRIX * vec4(VERTEX, 1.0)).xyz;
}

void fragment() {
	vec3 normal = normalize(NORMAL);
	
	vec4 base_color = texture(main_texture, UV);
	float noise_color = texture(noise_texture, UV).r;
	
	//handle clouds
	vec2 scroll = cloud_dir * TIME * scroll_speed;
	float cloud_color = texture(cloud_noise_texture , (world_position.xz * texture_scale) + scroll).r;
	
	float darkness = mix(1.0, noise_color, noise_strength);
	float clouds = mix(1.0, cloud_color, noise_strength);
    
    // 4. Apply modulation to the texture color
    ALBEDO = base_color.rgb * darkness * clouds * vec3(1, 2, 1);
    ALPHA = base_color.a; // Preserve texture alpha
	//ALBEDO = mix(ALBEDO.rgb, cloud_color.rgb, noise_strength);
}

//void light() {
//	// Called for every pixel for every light affecting the material.
//	// Uncomment to replace the default light processing function with this one.
//}
"

[sub_resource type="Gradient" id="Gradient_81xng"]
offsets = PackedFloat32Array(0.25641027, 0.974359)

[sub_resource type="FastNoiseLite" id="FastNoiseLite_6phgx"]
noise_type = 3
frequency = 0.02
fractal_octaves = 1
fractal_lacunarity = 5.0
fractal_gain = 20.0

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_81xng"]
noise = SubResource("FastNoiseLite_6phgx")
color_ramp = SubResource("Gradient_81xng")
seamless = true

[sub_resource type="Gradient" id="Gradient_6phgx"]
colors = PackedColorArray(0, 0, 0, 1, 0.10561416, 0.10561416, 0.10561416, 1)

[sub_resource type="FastNoiseLite" id="FastNoiseLite_81xng"]
noise_type = 2
frequency = 0.5
fractal_type = 3
fractal_lacunarity = 1.5
fractal_gain = 1.0
fractal_weighted_strength = 1.0

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_wqkje"]
noise = SubResource("FastNoiseLite_81xng")
color_ramp = SubResource("Gradient_6phgx")
in_3d_space = true

[resource]
render_priority = 0
shader = SubResource("Shader_tdedc")
shader_parameter/main_texture = ExtResource("2_e6ndn")
shader_parameter/noise_texture = SubResource("NoiseTexture2D_wqkje")
shader_parameter/cloud_noise_texture = SubResource("NoiseTexture2D_81xng")
shader_parameter/noise_strength = 0.5
shader_parameter/cloud_dir = Vector2(1, 0)
shader_parameter/scroll_speed = 0.5
shader_parameter/texture_scale = 0.2
shader_parameter/blend_sharpness = 4.0
